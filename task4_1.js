const colors = ["Red", "Green", "White", "Black"];
for (let i = 0; i < colors.length; i++) {
    // console.log(colors);
}
let text = colors.toString();
console.log(text);
const f = colors.join(", ");
console.log(f);
const e = colors.join(" + ");
console.log(e);